# proxy-cos-dns

```
  -bucketRegion string    
        桶的Region（必填）
  -cdnCheck
        是否检查cdn域名是否正常解析，默认为false
  -domainSuffix string
        域名（必填）
  -port int
        绑定端口，默认为3321 (default 3321)
  -secretID string
        腾讯云SecretID（必填）
  -secretKey string
        腾讯云SecretKey（必填）
```